# 快速开始指南

> 5分钟从零到跑通整个项目。用最通俗的方式理解这个系统。

---

## 可视化与 GUI（新）

不想只看命令行？项目自带三套可视化产物：

```bash
# ① GUI 操作台（推荐）：本地网页，点选工具/技能、填参数、发起真实调用
python gui/server.py
# 浏览器打开 http://127.0.0.1:8765
# 四个页签：工具台 / 技能台 / 统计看板 / MCP·JSON-RPC

# ② 审计看板：单文件 HTML，双击 assets/dashboard.html 即可（离线可用）
# 数据来自一次真实模拟会话的 stats.json，重新生成：python tools/gen_dashboard.py

# ③ 图表组：assets/charts/ 下 5 张 PNG（性能对比/流水线/权限/安全/流转）
# 再生成：python tools/gen_charts.py

# ④ 交互式架构图：assets/archify/ 下两张 HTML（双击即开，可缩放/搜索/演示模式）
```

> ⚠ GUI 的调用是**真实 OS 操作**（权限闸门、黑名单、保护路径全部生效）。
> 服务只绑定本机 127.0.0.1；演示时文件参数建议指向沙箱目录。

---

## 五组全链路演示（新）：项目最终跑起来长什么样

组3、组5 交付前，用**联调替身**把整条流水线跑起来（组4 是真实模块）：

```bash
python pipeline_demo/run_demo.py
```

一句话输入「整理 demo_task 文件夹 + 查重 + 备份」，屏幕上依次演出：

1. **组1** 解析话语 → `intent.json`（识别出 3 个目标动作）
2. **组2** 规划 → `plan.json`（4 个步骤，含一个需 ADMIN 的收尾命令）
3. **组3** 逐步执行：技能走 SkillLibrary、命令被拒后**请求用户确认 → 提权 ADMIN 重试**、
   工具名写错时用 list_tools **自愈纠错**（真实组4 在中间逐闸门放行）
4. **组5** 只读 JSON 文件出**审计报告**（`audit_report.json`），并核验与组3 的账目一致性

契约纪律：组1/组2 纯逻辑、组3 只走公开契约（Mock 换真实零改动）、组5 不 import 组4
任何代码只吃 JSON——替身代码本身就是《组间集成指南》的可运行示例，真实组3/组5
到位后**按接口替换即可**。测试见 `tests/test_pipeline_demo.py`（6 个）。

---

## 一句话说明

这是一个 **"AI操作系统的工具箱"** —— AI Agent 可以通过它来调用文件操作、执行命令、整理目录等OS能力，而且有权限控制、安全检查、调用统计等企业级功能。

---

## 第1步：确认环境

打开终端/命令行，输入：

```bash
python --version
```

确保显示 `Python 3.10` 或更高版本。（3.13 最佳）

---

## 第2步：进入项目目录

```bash
cd linux-agentic-os-group4
```

---

## 第3步：运行演示（30秒看效果）

```bash
python main.py
```

你会看到 8 步自动演示，输出类似：

```
============================================================
分组四：工具注册 + OS Skills（合并版演示）
============================================================

[1] 已注册工具列表
  - copy_file: 复制文件
  - move_file: 移动文件
  - run_command: 执行Shell命令
  ... (共11个，含3个MCP Mock工具)

[2] 权限验证
  user 调用 run_command: {'success': False, 'error': '权限不足...'}
  admin 调用 run_command: {'success': True, 'result': 'hello\n'}

[3] 文件操作
  写入: {'success': True}
  读取: {'success': True, 'result': 'Hello 分组四!'}

[4] OS Skill：整理目录
  {'success': True, 'result': {'total_moved': 4, ...}}

[5-8] ... 备份 / MCP Mock / JSON-RPC格式 / 统计报告

============================================================
演示结束
============================================================
```

**看到这个 = 项目能正常工作** ✅

---

## 第4步：快速验证（不需要 pytest）

```bash
python quick_check.py
```

会逐项检查核心功能并打印 [PASS] 或 [FAIL]：

```
=== 分组四：工具注册 + OS Skills 快速验证 ===

[工具注册]
  [PASS] 默认8个工具已注册
  [PASS] 工具总数正确 (8)
  [PASS] MCP Mock工具已注册 (3)

[权限控制]
  [PASS] USER无法调用ADMIN工具
  [PASS] ADMIN可以调用ADMIN工具

[安全机制]
  [PASS] 危险命令被拦截
  [PASS] 空路径被拒绝

[技能库]
  [PASS] 技能列表非空 (8个)
  [PASS] 整理目录技能可运行

[MCP协议]
  [PASS] 搜索Mock返回正确格式
  [PASS] 天气Mock返回正确格式

=== 验证完成: 12/12 通过 ===
```

**全 PASS = 核心功能正常** ✅

---

## 第5步：完整测试（需要 pytest）

如果你安装了 pytest：

```bash
pip install pytest
python -m pytest tests/test_all.py -v
```

预期输出：`31 passed in 0.xx秒`

> 如果没有 pytest，跳过这步也没关系。前两步已经足够验证功能了。

---

## 出错了怎么办？

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| `ModuleNotFoundError: No module named 'src'` | Python 找不到 src 包 | 确保 cd 进了项目根目录 |
| `No module named 'pytest'` | 没装 pytest | `pip install pytest` 或跳过第5步 |
| 中文乱码 | 终端编码问题 | Windows 用 `chcp 65001` 切换UTF-8 |

详细排查请查看 [RUN_GUIDE.md](./RUN_GUIDE.md)。

---

## 通俗理解：这个系统在做什么？

想象你是一个 **AI助手**（比如 ChatGPT），用户对你说：

> "帮我整理下载文件夹里的所有文件"

作为AI，你不能直接操作电脑的文件系统。但有了我们的模块，你可以这样：

```
AI: 我来调用 SkillLibrary 的 "organize_downloads" 技能！
    ↓
SkillLibrary: 好的，我需要：
    1. 先 list_directory（列出所有文件）   ← 调用 ToolRegistry
    2. 再 create_folder（创建子文件夹）   ← 调用 ToolRegistry
    3. 然后 move_file（移动每个文件）     ← 调用 ToolRegistry × N
    ↓
ToolRegistry: 收到请求！先检查权限 → 通过 → 执行 → 返回结果
    ↓
default_tools: 真正操作文件系统 shutil.move()
    ↓
操作系统: 完成磁盘上的文件移动 ✅
AI: "好了！我已经把你的下载文件夹整理好了，
     PDF放到了文档文件夹，图片放到了图片文件夹..."
```

这就是我们模块的价值——**让 AI 能够安全、有权限控制地操作电脑**。
